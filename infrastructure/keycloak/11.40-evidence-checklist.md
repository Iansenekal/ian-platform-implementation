# 11.40 Evidence Checklist

- `openssl s_client` output showing successful LDAPS certificate validation.
- `ldapsearch` LDAPS bind result with user DN/memberOf evidence (sanitized).
- `/opt/idp/config/ad-ldaps.yaml` and `/opt/idp/config/group-mapping.yaml` presence and permissions.
- `/opt/idp/secrets/ad_bind_dn` and `/opt/idp/secrets/ad_bind_password` permissions evidence.
- Token decode evidence showing expected mapped `groups` claim.
- Boundary evidence showing no inbound LDAP/LDAPS exposure on AI-DATA01.
