# 82.80 Evidence Checklist

Capture and retain the following for 82.80 sign-off:

- `82.80-verification-run.sh` output log from `/opt/ollama/logs/verification/`
- `ufw status verbose` and `ufw status numbered`
- `ss -tulpen` listener proof
- `systemctl status ollama --no-pager` and recent `journalctl -u ollama`
- Local tags output (`127.0.0.1:11434/api/tags`)
- AI-DATA01 remote tags success proof
- AI-FRONTEND01 proof matching policy (pass/fail by design)
- Non-allowlisted host failure proof
- `ollama list` output
- GPU sanity outputs (`lspci`, `lsmod`, `/dev/dri`, `/dev/kfd`, `rocm-smi` if installed)

Evidence retention: minimum 180 days, aligned to `06.40`.
