# 60.10 architecture/data-flow inputs

VOICE_UI_HOST=ui.<domain>
GATEWAY_HOST=api.<domain>
NEXTCLOUD_HOST=nextcloud.<domain>
WORKER_MODE=cpu|gpu|hybrid
QUEUE_ENABLED=true
REVIEW_GATE_ENABLED=true
GROUP_CLAIM_NAME=groups
REQUIRED_JOB_FIELDS=sensitivity_label,retention_class,indexing_rule
AUDIT_EVENT_SET=upload,job_lifecycle,review,indexing,purge
NO_PUBLIC_ENDPOINTS=true
