# 20.30 Evidence Checklist

- OIDC discovery endpoint output for `id.<domain>/.well-known/openid-configuration`.
- Browser evidence of successful UI -> IdP -> UI login redirect flow.
- Cookie flag evidence showing `Secure`, `HttpOnly`, and `SameSite`.
- Token claim evidence showing canonical `groups` claim (redacted).
- Negative test evidence for unauthorized project access (`403`) and logout session invalidation.
