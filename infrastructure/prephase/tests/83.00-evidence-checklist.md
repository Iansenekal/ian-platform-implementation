# 83.00 Evidence Checklist

Capture and preserve the following outputs:

- DNS resolution results from frontend, data, and llm nodes.
- Route and ping sanity outputs from frontend/data nodes.
- Frontend -> gateway success (`nc` + `curl` health) output.
- Frontend -> AI-DATA01 blocked datastore ports (`5432`, `9200`) failure proof.
- Data -> LLM (`11434`) success and `/api/tags` JSON output.
- Frontend -> LLM policy proof (pass or fail per policy).
- TLS certificate subject/issuer/dates output for gateway/frontend internal hosts.
- `ss -tulpen` listener snapshot on all three nodes.
- `ufw status verbose` and `ufw status numbered` on all three nodes.
- Full runner log: `infrastructure/prephase/tests/logs/83.00-connectivity-*.log`.

Store evidence per `06.40` preservation requirements.
