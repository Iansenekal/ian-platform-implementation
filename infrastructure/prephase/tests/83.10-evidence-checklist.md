# 83.10 Evidence Checklist

Capture and preserve the following outputs:

- `resolvectl status` output from AI-DATA01, AI-FRONTEND01, and LLM.
- Forward lookup outputs (`getent hosts`, `dig +short`) for:
  - `ai-data01.lab.local`
  - `ai-frontend01.lab.local`
  - `ai-llm01.lab.local`
  - `api.lab.local`
  - `ui.lab.local`
- Reverse lookup outputs (`dig -x`) for core VM IPs.
- Targeted DNS-server query outputs (`dig @<dns_server>`).
- Windows DNS evidence:
  - `Resolve-DnsName` output for `api`/`ui`/core hosts
  - Forwarders tab screenshot or exported config note
  - Recursion policy note
- Full runner log: `infrastructure/prephase/tests/logs/83.10-dns-*.log`.

Store evidence per `06.40` requirements.
