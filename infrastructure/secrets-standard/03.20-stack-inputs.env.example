# Stack identifier (folder under /opt)
STACK_NAME="ai-gateway"

# Service group allowed to read non-key secret files
SERVICE_GROUP="ai-gateway"

# Optional: regex to narrow verification scans to one stack path
STACK_PATH="/opt/ai-gateway"

# Default rotation target in days (for evidence/readme metadata)
ROTATION_DAYS="90"
