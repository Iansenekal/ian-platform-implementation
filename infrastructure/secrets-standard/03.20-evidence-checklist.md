# 03.20 Evidence Checklist

- Capture `stat` output for files under `/opt/<stack>/secrets/`.
- Capture scan output proving no hardcoded secrets outside `/opt/<stack>/secrets/`.
- Capture ownership/permissions for `/opt/<stack>/{secrets,env,compose}`.
- Record rotation interval, custodian names, and break-glass process reference.
