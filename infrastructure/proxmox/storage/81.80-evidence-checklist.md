# 81.80 Storage Implementation Evidence Checklist

- [ ] Output captured: `lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINT,MODEL,SERIAL`
- [ ] Output captured: `df -hT`
- [ ] Output captured: `pvesm status`
- [ ] Screenshot captured: `Datacenter -> Storage` showing `iso-store`, VM datastore, and `backup-store`.
- [ ] ISO list screenshot captured showing Ubuntu 24.04 image.
- [ ] LVM-thin proof captured (`lvs`, `vgs`) OR ZFS proof captured (`zpool status`, `zfs list`).
- [ ] Naming conventions validated (`iso-store`, `vm-disks` or `zfs-vm`, `backup-store`).
