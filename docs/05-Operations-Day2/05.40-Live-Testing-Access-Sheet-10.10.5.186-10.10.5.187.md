# Live Testing Access Sheet (10.10.5.186 / 10.10.5.187)

Purpose: quick reference for live testing access on the two active hosts.

## Hosts

| Hostname | IP | Role |
|---|---|---|
| `iai-frontend01` | `10.10.5.186` | Frontend / reverse-proxy boundary |
| `iai-backend01` | `10.10.5.187` | Backend / IdP runtime host |

## Confirmed Access Accounts

| System | URL / Endpoint | Username | Password | Notes |
|---|---|---|---|---|
| SSH (frontend) | `ssh choice@10.10.5.186` | `choice` | (your current VM password) | Confirmed in your session |
| SSH (backend) | `ssh choice@10.10.5.187` | `choice` | (your current VM password) | Confirmed in your session |
| Keycloak Admin (dev stack) | `http://10.10.5.187:8080` | `admin` | `change-me-in-env` (default) | From `infrastructure/keycloak/docker-compose.yml`; change immediately for non-lab use |

## Confirmed Service URLs for Live Testing

| Service | URL | Host |
|---|---|---|
| Keycloak (UI/API, direct) | `http://10.10.5.187:8080` | backend |
| Keycloak management health | `http://10.10.5.187:9000/health/ready` | backend |
| OIDC discovery (master realm) | `http://10.10.5.187:8080/realms/master/.well-known/openid-configuration` | backend |
| OIDC discovery (platform realm, if imported) | `http://10.10.5.187:8080/realms/platform/.well-known/openid-configuration` | backend |

## DNS/FQDN Targets Used by This Project

These are the canonical URLs in project templates (usable when DNS and proxy are configured to your current hosts):

| Logical endpoint | FQDN |
|---|---|
| UI | `https://ui.lab.local` |
| API | `https://api.lab.local` |
| IdP | `https://id.lab.local` |

## Services Marked Optional / Not Confirmed as Running on 186/187

- Nextcloud
- OpenSearch / Dashboards
- n8n
- Windmill
- Whisper transcription runtime
- Workflow/eSign runtime engine
- SMTP relay runtime

If you want these added with concrete URLs and credentials, first confirm each runtime is deployed and expose the active compose/env used on `10.10.5.186` / `10.10.5.187`.

## Quick Validation Commands

Run from backend (`10.10.5.187`):

```bash
curl -sS http://10.10.5.187:9000/health/ready
curl -sS http://10.10.5.187:8080/realms/master/.well-known/openid-configuration | head -n 20
```

Run from frontend (`10.10.5.186`):

```bash
nc -vz 10.10.5.187 8080
```

## Security Note

This sheet contains sensitive access information. Store in a restricted path and replace default credentials before production usage.
